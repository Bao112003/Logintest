name: TLU Login Failure

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  login-failure:
    name: Test invalid login
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CI: "true"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Run invalid login test
        run: bash mvnw --batch-mode --no-transfer-progress -Dtest=tlutest#testLoginFailure test

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: tlu-login-failure-report
          path: target/surefire-reports/
          if-no-files-found: warn
          retention-days: 14
